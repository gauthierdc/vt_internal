// Converti depuis le Client Script ERP 'Feuille de temps liste' (Timesheet / List).
// Source de vérité : ce fichier (versionné). Le record DB a été supprimé.

Object.assign(frappe.listview_settings['Timesheet'] = {
    hide_name_column: true,
})
